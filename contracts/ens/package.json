{
  "name": "@rf/contracts-ens",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "check:balance": "hardhat run scripts/check-balance.ts --network sepolia",
    "compile": "hardhat compile",
    "typecheck": "tsc --noEmit",
    "test": "npm run test:unit && npm run test:int",
    "test:unit": "hardhat test test/unit/*.test.ts --network hardhat",
    "test:int": "hardhat test test/integration/*.test.ts --network hardhat",
    "onboard": "hardhat run scripts/onboard.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "typescript": "^5.6.0"
  }
}
