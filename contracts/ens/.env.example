# The network the business profiles and eligibility passes live on.
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Private key of the funded account that registers names and writes records.
# Real values live in .env, which is never tracked.
SEPOLIA_PRIVATE_KEY=
