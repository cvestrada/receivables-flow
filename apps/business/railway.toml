# Railway service config for the business portal. Point the service's "config path" at this file.
[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile"
buildArgs = { APP = "business" }

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
